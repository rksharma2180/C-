using System;

namespace Constants
{
       /*
            Constatnts are immutable values which are known at compile time and do not change
            for the lifetime of the program.
        */
    class Program
    {
        // Constants as fields
        const double pi = 3.14159265359;
        const int weeks = 52, months = 12;
        
        static void Main(string[] args)
        {
            Console.WriteLine(""); //cw double tab for this statement

        }
    }
}
